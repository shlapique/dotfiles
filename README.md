# 🐰 My dotfiles :)

## Configure notes

before config

```
sudo zypper rm --clean-deps tuned
```

to avoid conflicts with tlp

and

```
rm ~/{.bashrc,.vimrc}
```

and to avoid conflicts with my x11 configs:

```
sudo rm /etc/X11/xorg.conf.d/00-keyboard.conf
```
